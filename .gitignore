# Local configuration and data (never commit)
config.yaml
config.yml
*.db
*.db-wal
*.db-shm
data/

# Build output
/rmm
/rmm-linux-arm64
/dist/
# NOTE: web/dist is NOT ignored. In Phase 1 the frontend is hand-written there
# and embedded via go:embed, so it is source and must be committed.

# macOS
.DS_Store

# Local deploy target (host, user, paths)
deploy/deploy.env

# Local editor / harness config
.claude/

# Runtime state written to cwd during local dev or next to the settings path on
# the Pi. Never commit these: miners.json holds miner IPs/hostnames, and the
# others are per-install state, not source.
thresholds.json
miners.json
history.gob
*.gob

# Local session handoff (names the Pi)
handoff.md

# Evidence subsystem: local data (DB, documents), config, binaries
mining-evidence/
/data/
evidence.yaml
/rmm-evidence
/rmm-evidence-linux-arm64
